# Add CVE IDs here only after reviewing them - one per line, with a comment
# explaining why it's accepted. Example:
# CVE-2023-12345  # base nginx image, no exploitable path in our config, patch pending upstream
